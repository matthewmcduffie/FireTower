<UserControl x:Class="ShippingGuard.Tray.Views.AppListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="{Binding AgentStatus}" Style="{StaticResource CaptionStyle}"
                       VerticalAlignment="Center" DockPanel.Dock="Right" Margin="8,0,0,0"/>
            <Ellipse Width="8" Height="8" Margin="0,0,4,0" DockPanel.Dock="Right" VerticalAlignment="Center">
                <Ellipse.Style>
                    <Style TargetType="Ellipse">
                        <Setter Property="Fill" Value="#FF6B6B"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsConnected}" Value="True">
                                <Setter Property="Fill" Value="#3DDC84"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Ellipse.Style>
            </Ellipse>
            <TextBlock Text="ShippingGuard" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center"/>
        </DockPanel>

        <ItemsControl Grid.Row="1" ItemsSource="{Binding Apps}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource CardBorder}" Margin="0,0,0,10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Column="0" Grid.Row="0" Orientation="Horizontal">
                                <Ellipse Width="10" Height="10" Margin="0,0,8,0" VerticalAlignment="Center">
                                    <Ellipse.Fill>
                                        <SolidColorBrush Color="{Binding HealthColor}"/>
                                    </Ellipse.Fill>
                                </Ellipse>
                                <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" FontSize="14"/>
                                <TextBlock Text="{Binding HealthLabel}" Margin="8,0,0,0"
                                           Style="{StaticResource CaptionStyle}" VerticalAlignment="Center"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1" Grid.Row="0" Orientation="Horizontal">
                                <Button Content="Start"   Margin="4,0,0,0" Command="{Binding StartCommand}"/>
                                <Button Content="Stop"    Margin="4,0,0,0" Command="{Binding StopCommand}"/>
                                <Button Content="Kill"    Margin="4,0,0,0" Command="{Binding KillCommand}"/>
                                <Button Content="Maint"   Margin="4,0,0,0" Command="{Binding ToggleMaintenanceCommand}"
                                        ToolTip="Toggle maintenance mode"/>
                            </StackPanel>

                            <StackPanel Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="1" Margin="18,6,0,0">
                                <TextBlock Text="{Binding LastAction}" Style="{StaticResource CaptionStyle}"/>
                                <TextBlock Text="{Binding LastError}"  Style="{StaticResource CaptionStyle}"
                                           Foreground="#FF6B6B"
                                           Visibility="{Binding LastError, Converter={StaticResource NullToCollapsed}}"/>
                                <TextBlock Style="{StaticResource CaptionStyle}">
                                    <Run Text="Restarts: "/>
                                    <Run Text="{Binding RestartCount}"/>
                                </TextBlock>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
