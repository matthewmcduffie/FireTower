<UserControl x:Class="FireTower.Tray.Views.ProvidersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,16">
            <Button DockPanel.Dock="Right" Content="Refresh" Command="{Binding RefreshCommand}"/>
            <TextBlock Text="Providers" Style="{StaticResource HeadlineTextStyle}"/>
        </DockPanel>

        <Border Grid.Row="1" Style="{StaticResource CardBorderStyle}" Padding="0">
            <DataGrid ItemsSource="{Binding Providers}" BorderThickness="0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Provider" Binding="{Binding FriendlyName}" Width="160"/>
                    <DataGridTextColumn Header="Version" Binding="{Binding Version}" Width="100"/>
                    <DataGridCheckBoxColumn Header="Available" Binding="{Binding IsAvailable}" Width="80"/>
                    <DataGridTextColumn Header="Unavailable Reason" Binding="{Binding UnavailableReason}" Width="200"/>
                    <DataGridTextColumn Header="Capabilities" Binding="{Binding Capabilities}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
