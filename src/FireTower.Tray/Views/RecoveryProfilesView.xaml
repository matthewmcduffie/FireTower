<UserControl x:Class="FireTower.Tray.Views.RecoveryProfilesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,8">
            <Button DockPanel.Dock="Right" Content="Refresh" Command="{Binding RefreshCommand}"/>
            <TextBlock Text="Recovery Profiles" Style="{StaticResource HeadlineTextStyle}"/>
        </DockPanel>
        <TextBlock Grid.Row="1" Text="Profiles are edited as configuration JSON on the Settings page." Style="{StaticResource CaptionTextStyle}" Margin="0,0,0,16"/>

        <ScrollViewer Grid.Row="2">
            <ItemsControl ItemsSource="{Binding Profiles}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,12">
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="15"/>
                                <TextBlock Text="{Binding Id}" Style="{StaticResource CaptionTextStyle}" Margin="0,0,0,8"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="COOLDOWN" Style="{StaticResource CaptionTextStyle}"/>
                                        <TextBlock Text="{Binding CooldownSeconds, StringFormat='{}{0}s'}"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="1">
                                        <TextBlock Text="MAX ATTEMPTS" Style="{StaticResource CaptionTextStyle}"/>
                                        <TextBlock Text="{Binding MaxRestartAttempts}"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="RETRY WINDOW" Style="{StaticResource CaptionTextStyle}"/>
                                        <TextBlock Text="{Binding RetryWindowSeconds, StringFormat='{}{0}s'}"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="3">
                                        <TextBlock Text="GRACEFUL FIRST" Style="{StaticResource CaptionTextStyle}"/>
                                        <TextBlock Text="{Binding PreferGracefulRestart}"/>
                                    </StackPanel>
                                </Grid>
                                <TextBlock Margin="0,8,0,0" Text="{Binding EscalationSequence, StringFormat='Escalation: {0}'}"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
