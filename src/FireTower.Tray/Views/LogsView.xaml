<UserControl x:Class="FireTower.Tray.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Logs" Style="{StaticResource HeadlineTextStyle}" Margin="0,0,0,16"/>

        <DockPanel Grid.Row="1" Margin="0,0,0,16">
            <Button DockPanel.Dock="Right" Content="Refresh" Command="{Binding RefreshCommand}"/>
            <TextBox DockPanel.Dock="Left" Width="160" Margin="0,0,8,0"
                     Text="{Binding ComponentFilter, UpdateSourceTrigger=PropertyChanged}"
                     ToolTip="Filter by component"/>
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" ToolTip="Search message text"/>
        </DockPanel>

        <Border Grid.Row="2" Style="{StaticResource CardBorderStyle}" Padding="0">
            <DataGrid ItemsSource="{Binding Logs}" BorderThickness="0" FontFamily="{StaticResource MonoFontFamily}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Time" Binding="{Binding Timestamp}" Width="160"/>
                    <DataGridTextColumn Header="Level" Binding="{Binding Level}" Width="80"/>
                    <DataGridTextColumn Header="Component" Binding="{Binding Component}" Width="220"/>
                    <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
