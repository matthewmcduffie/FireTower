<UserControl x:Class="FireTower.Tray.Views.HealthProfilesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,8">
            <Button DockPanel.Dock="Right" Content="Refresh" Command="{Binding RefreshCommand}"/>
            <TextBlock Text="Health Checks" Style="{StaticResource HeadlineTextStyle}"/>
        </DockPanel>
        <TextBlock Grid.Row="1" Text="Profiles are edited as configuration JSON on the Settings page." Style="{StaticResource CaptionTextStyle}" Margin="0,0,0,16"/>

        <ScrollViewer Grid.Row="2">
            <ItemsControl ItemsSource="{Binding Profiles}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,12">
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="15"/>
                                <TextBlock Text="{Binding Id}" Style="{StaticResource CaptionTextStyle}" Margin="0,0,0,8"/>
                                <ItemsControl ItemsSource="{Binding Checks}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,4">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0" Text="{Binding Id}"/>
                                                <TextBlock Grid.Column="1" Text="{Binding Kind}" Margin="12,0" Style="{StaticResource CaptionTextStyle}"/>
                                                <TextBlock Grid.Column="2" Text="{Binding IntervalSeconds, StringFormat='every {0}s'}" Margin="12,0" Style="{StaticResource CaptionTextStyle}"/>
                                                <TextBlock Grid.Column="3" Text="{Binding Enabled, StringFormat='Enabled: {0}'}" Style="{StaticResource CaptionTextStyle}"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
